# 视频到投资体系的沉淀工作流

## 原则

视频是输入源，Obsidian 是思考仓库，网页是公开表达层。AI 负责整理，人负责判断。

## 流程

1. 录制视频或音频。
2. 转写成逐字稿。
3. AI 按模板提取结构化信息。
4. 写入 Obsidian 的 `00_Inbox`。
5. 人工确认后拆分到概念、公司、读书和认知日志。
6. 给可公开内容添加 `publish: true`。
7. 网页系统读取公开内容并生成页面。
8. 需要发短视频时，HTML 只做预览，最终优先输出 `png/` 图片卡，不再手动截图。

## 入库字段

- `title`：标题
- `type`：内容类型
- `publish`：是否公开
- `status`：草稿、常青、待验证
- `source`：视频、书籍、演讲、对话
- `related_concepts`：关联概念
- `related_companies`：关联公司
- `related_books`：关联书籍
- `compliance_note`：合规说明

## 内容产物

- 正式文章
- 概念卡片
- 公司研究更新
- 指标跟踪问题
- 认知更新日志
- 短视频标题和摘要

## 视图导出

每期视频的 HTML 页面都尽量保留在资产文件夹里，最终发文时优先产出图片卡：

```text
优先：用图片类技能直接生成 png 卡片
备选：用本地导出脚本把 slides.html 转成 png
```

导出结果默认会落在同目录的 `png/` 文件夹里，后面你可以：

- 直接挑图发文
- 或者把图片再拼成图文
- 或者把 png 当作视频剪辑里的静态素材
