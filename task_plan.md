# 红利研报阅读与投资框架映射

## 目标
阅读 `factors/hongli_factor` 下的研报，结合仓库中现有投资理论，提炼值得吸收的观点、可研究的因子与简易选股回测方案。2026-09-23 起，用户授权实现小规模 SQLite 的 iFinD 历史回补、日更和红利择时信号，不引入 MySQL/PostgreSQL。

## 阶段
- [x] 1. 盘点研报与项目结构
- [x] 2. 定位并阅读当前投资理论材料
- [ ] 3. 提取研报正文、公式、图表与回测口径
- [ ] 4. 对照现有理论，整理可学习点与分歧
- [ ] 5. 形成后续因子与模型讨论底稿
- [x] 6. 接入 iFinD 历史行情与历史指数股息率，并写入 SQLite
- [x] 7. 按预注册规则生成日更收益差、Z-score 与仓位观察信号
- [x] 8. 验证离线命令、数据缺失保护与日更入口（实际 iFinD 登录/取数待用户在环境变量配置凭据后首次运行）
- [x] 9. 扩展指数估值字段，并实现历史策略与定投信号可视化网页（PS/PCF 将在下一次行情刷新时写入；标准差通道等待完整共同历史样本）

## 关键约束
- 区分研报原结论、我的推断和后续建议。
- 对收益结论重点检查样本期、调仓频率、行业暴露、交易成本、幸存者偏差与未来函数。
- 本轮不写选股程序，不擅自决定数据源或股票池。

## 错误记录
| 错误 | 尝试 | 处理 |
|---|---:|---|
| 系统未安装 `pdfinfo` / `pdftotext` | 1 | 改用 Python 的 `pypdf` / `pdfplumber` 提取，必要时渲染关键页核对 |
| Python 未安装 `pypdf` / `pdfplumber` | 1 | 检查现有 PDF 引擎（PyMuPDF 等）；若均无，再申请安装最小依赖 |
| PowerShell 默认读取 Markdown 出现中文乱码 | 1 | 后续显式使用 UTF-8 编码读取 |
| PyMuPDF 提取脚本打印元数据时遇到 GBK 不支持字符 | 1 | 正文已在报错前成功写出；后续不直接打印特殊字符元数据 |
| PowerShell 解析内嵌 Node/SQL 检查命令失败 | 1 | 改用 PowerShell 单引号包裹 Node 程序，避免 `SELECT` 中的双引号与括号被 Shell 预解析 |
| Node `-e` 程序的引号仍在传参时被剥离 | 2 | 不再用内嵌 SQL 做库审计；改用项目已有的 `npm run report` 查看最新共同观测，并从同步器定义审阅数据覆盖 |
| `npm run report` 无法启动：缺少 `xlsx` 依赖 | 1 | 只读检查无需安装依赖；改用现有 Python 的内置 `sqlite3` 审阅本地数据库。后续若要运行同步/回测，应先在该目录执行 `npm ci` 或 `npm install` |
| Git 状态检查被 `safe.directory` 所有者保护拦截 | 1 | 未修改 Git 全局配置；本轮无须依赖 Git 状态，保留现有工作区内容 |
| 信号脚本在 iFinD 首次同步前因缺表输出 traceback | 1 | 将其改为结构化 `stale` 响应，明确要求先运行 iFinD 同步，不产生伪信号 |
| README 日更任务说明补丁未命中原文 | 1 | 先读取 Windows 日更任务章节的实际措辞，再做精确更新；不重复使用同一上下文补丁 |
| SQLite 表清单检查中的 SQL 字符串引号被错误构造 | 1 | 不重复该嵌套引号写法；改用无需 SQL 字符串字面量的只读汇总查询 |
| `npm run sync` 无法加载 `xlsx` | 1 | `package-lock.json` 已锁定该依赖但本地 `node_modules` 为空；在 hongli_factor 目录执行一次 `npm.cmd ci` 后重试 |
| 一次性替换前端脚本的补丁格式不被 `apply_patch` 接受 | 1 | 不重复同一复合补丁；改为两个连续补丁，先删除明确目标脚本、再立即写入完整交互版本 |

## 当前决策
- 现有环境已安装 PyMuPDF，使用它进行全文提取与关键页面渲染，无需安装依赖。
- 将当前理论归纳为“家庭资产层—策略层—公司层”三层，以避免把红利因子直接等同于完整投资理论。
- 研报按“总框架—择时—行业—选股—风险与可复现性”拆解。
- 2026-09-23：现有方案先保持第一阶段仅研究 `000922.CSI` 的整体仓位；用户新增 iFinD 数据后，优先验证其历史股息率口径与 point-in-time 可用性，再决定是否用它替换仅有最新快照的公开估值源。
- iFinD 登录遵循用户给出的 PriceServers-THS 已验证模式（`THS_iFinDLogin`、成功码 `0`、结果 `.data`），但凭据只从 `THS_USERNAME` / `THS_PASSWORD` 环境变量读取。
