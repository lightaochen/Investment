# 工作进度

## 2026-09-02
- 已阅读 PDF 与文件规划技能说明。
- 已盘点目标目录，确认只有 1 篇广发证券红利资产研究 PDF。
- 已建立本次阅读分析的计划与发现记录。
- 已定位现有投资理论候选材料。
- PDF 命令行提取工具不可用，记录后将切换 Python 库。
- 首次批量读取现有理论材料发生终端编码乱码；已记录，下一次显式采用 UTF-8。
- `pypdf` 与 `pdfplumber` 也未安装，准备检查系统中已有的其他 PDF 引擎。
- 已用 UTF-8 成功读取六篇核心投资框架材料，并整理为家庭资产、策略、公司三层。
- 已确认 PyMuPDF 可用，可继续完成研报全文提取和页面核验。
- 已完成 34 页 PDF 的正文提取，并通过关键词索引定位三大章节及主要结论。
- 已通读择时与行业章节正文，提炼宏观环境、分红事件、险资资金、A/H 比较和四类行业驱动。

## 2026-09-23
- 已复核 `factors/hongli_factor` 的两份研究方案、数据采集脚本、SQLite 设计和研报择时正文。
- 确认现有代码完成了公开数据的采集与原始快照留存，但还未进入策略信号和回测实现阶段；下一步将以 iFinD 的指数历史股息率字段完成数据可得性审计，并据研报收紧为“主要做卖点”的预注册规则。
- 已审计本地数据库覆盖：价格和国债历史足够长，指数股息率仅有 20 个近期观测，故当前数据无法回测；iFinD 是填补该缺口的候选数据源。
- 用户授权进入实现阶段：将参考 PriceServers-THS 的 iFinD 登录/`.data` DataFrame 模式，新增 SQLite 历史回补、日更与信号构建脚本；账号凭据不复制、不写入代码。
- 离线校验发现信号脚本在首次 iFinD 同步前需要更明确的数据缺失保护，已改为输出结构化 stale 状态而非 traceback。
- 信号生成同时校验 iFinD 指数表和 10Y 国债表：任一缺失均停止并标记 stale，避免输出以旧债券或缺失债券为基础的信号。
- 已完成离线交付验证：两份 Python 脚本语法通过、`--help` 可用、PowerShell 日更入口可解析；未使用参考项目中的明文凭据，也未执行实际 iFinD 网络请求。首次实取须由用户在环境变量配置账号后进行。
- 按用户偏好改为优先读取 `config/ifind.env`；该本地凭据文件在 `.gitignore` 中，字段为空时才回退到环境变量。
- 用户首次小范围同步成功，`ifind_index_daily` 中六个指数均获得 17 个交易日股息率；发现两个非主基准指数在 2026-09-23 缺少行情收盘价，已增强同步摘要以显示此类缺口并将运行标为 partial。
- 已开始第 9 阶段：iFinD 行情字段扩展为 PS/PCF，并新增本地只读策略看板，计划展示历史估值、股债收益差标准差通道、500 日均线趋势门控与月度手动定投参考。
- 本地看板 API 已针对现有 SQLite 联调通过：可读取 6 个指数和 `000922.CSI` 截至 2026-09-23 的数据；历史 Z-score 会在股息率与国债共同样本达到最小窗口后自动展示。
- 已审计用户历史回补后的实际覆盖：行情已补齐大部分区间，但历史股息率只有 17 天、PS/PCF 尚未写入。下一步应避免重复全量行情请求，单独批量补 `THS_BD` 历史股息率，再用新版本刷新 PS/PCF 字段。
- 已诊断用户全量 THS_BD 回补中断：Ctrl+C 造成一次 `KeyboardInterrupt`，后续任务在 2022-03-21 后发生 iFinD `-1010` 会话失效；本轮只记录原因与改进方向，未改动同步行为。
- 当前数据复查：主指数历史股息率和国债已足够生成 Z-score；PS/PCF 为 iFinD 返回的全空列，信号表待重建，失败队列仍保留历史会话失效记录。
- 已确认 `npm run sync` 的 `xlsx` 报错来自本地未安装 Node 依赖，lockfile 完整；无需更改代码，只需首次执行 `npm.cmd ci`。
- 根据用户的可读性反馈，已把网页主体改为中文无衬线字体并提高最小字号、图表刻度、标签对比度和控件尺寸；深色青蓝视觉方向保持不变。
- 已为两张折线图加入悬停十字线与日期详情浮层、滚轮缩放、按住拖拽平移及日/周/月/自动颗粒度选择；前端脚本已通过 Node 语法检查。

## 2026-09-24
- 已在“历史估值轨迹”和“股债收益差 · 标准差通道”标题右侧各加入独立的观察范围下拉框，支持近 1 年、近 3 年、近 5 年、近 10 年、近 20 年及全部历史。选择范围仅重置对应图表的展示窗口，不会改动数据或信号计算；滚轮缩放、拖拽平移和颗粒度选择保持可用。
- 已诊断非主指数的标准差图为空：非主指数的原始股息率与国债重叠数据足够，但现有信号构建器仅为 `primary_signal_index=000922.CSI` 写入 `red_dividend_signal_daily`。需另行实现全指数构建模式才会在页面显示。
- 已按现有 `--index-code` 参数为六个指数逐一生成并写入 `red_dividend_signal_daily`；六个指数均成功生成至 2026-09-23。信号表现有覆盖行数分别为 3,446、3,446、3,446、4,407、1,542、826，网页切换任一指数均可读取收益差曲线。
- 已将“观察指数”下拉框从代码与日期范围改为“中文全称 · 代码”，并对当前六个中证指数建立名称映射；JavaScript 语法与 Git diff 格式检查均已通过。
- 已在页面底部加入“基础定投、建议金额与建议仓位”的计算说明，明确金额公式为 `round(B × T × V)`，并标注当前金额倍率与图表悬停仓位为两套独立口径，避免误读为同一规则。
